# ROLL Environment Configuration

# Use CPU for IPC serialization to avoid CUDA IPC permission issues in containers
# Set to "false" to use CUDA IPC (requires SYS_PTRACE capability in Docker)
USE_CPU_IPC=true
